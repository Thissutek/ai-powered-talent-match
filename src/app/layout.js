// src/app/layout.js
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata = {
  title: 'TalentMatchAI - AI-Powered Talent Matching',
  description: 'Revolutionize your recruitment process with AI-driven candidate assessments and intelligent matching',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}